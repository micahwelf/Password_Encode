project PasswordEncode is

   -- for Main use ("passwordencode.adb");
   for Library_Name use "passwordencode";
   for Library_Dir use "lib";
   for Library_Version use "0.1";

   package Builder is
      for Default_Switches ("ada") use ("-j3");
   end Builder;

end PasswordEncode;

